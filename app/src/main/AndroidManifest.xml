<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".category_contents.relation.contents_relation"></activity>
        <activity android:name=".category_contents.myself.contents_myself" />
        <activity android:name=".category_contents.gender.contents_gender" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity android:name=".category_contents.family.contents_family" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".FaceRecognitionAcitivity" />
        <activity android:name=".TargetDetailReportActivity" />
        <activity android:name=".LoginNextActivity" />
        <activity android:name=".category_contents.myself.category_contents_myself" />
        <activity android:name=".category_contents.relation.category_contents_relation" />
        <activity android:name=".category_contents.gender.category_contents_gender" />
        <activity android:name=".category_contents.family.category_contents_family" />
        <activity android:name=".report.writingSolution" />
        <activity android:name=".report.report_detail" />
        <activity android:name=".report.report_menu" />
        <activity android:name=".MainActivity" />
        <activity android:name=".category_menu" />
        <activity android:name=".community_detail" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>